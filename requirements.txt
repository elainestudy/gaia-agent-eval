# Direct dependencies only -- pip resolves everything these need on their own.
# No version pins: this project targets whatever recent version each package
# currently ships. Install with `pip install -r requirements.txt`.

# Agent core: LangChain tool-calling loop around Gemini
langchain
langchain-community
langchain-core
langchain-google-genai

# Tools
duckduckgo_search       # search_internet
huggingface_hub         # tools/file_router.py Hub-fallback attachment download
youtube_transcript_api  # get_youtube_transcript
lxml                    # tools/fetch_page.py Wayback Machine content extraction
chess                   # imported directly by tools/run_code.py for deterministic
                        # chess-puzzle move/board verification (incl. driving stockfish)

# Attachment parsing (tools/file_router.py)
pandas                  # spreadsheet attachments
openpyxl                # pandas' .xlsx read engine
faster-whisper          # audio transcription

# Local video analysis (tools/video_local.py)
yt-dlp

# Misc
python-dotenv
requests
python-dateutil
